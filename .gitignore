# Foundry lock file (created when Foundry is running)
*.lock

# Compiled compendium packs are build output - pack-source/ (YAML) is the source of truth.
# Run scripts/pack-all.sh to regenerate before testing in Foundry.
packs/**

# VS Code workspace
*.code-workspace

# Windows shortcuts
*.lnk

# OS clutter
Thumbs.db
desktop.ini
.DS_Store
